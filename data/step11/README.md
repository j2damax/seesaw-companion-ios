# Step 11 — Round-Trip Latency Analysis

End-to-end round-trip estimates are derived from Step 5–8 metrics (generation latency + TTS duration + VAD listen window). No separate artifact files — analysis is documented in `../results_template.md` §11 and visualised in `../step12/charts/fig12_9_roundtrip_latency.png`.
